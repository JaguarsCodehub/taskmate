import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const NotificationProvider = () => {
    return (
        <View>
            <Text>NotificationProvider</Text>
        </View>
    )
}

export default NotificationProvider

const styles = StyleSheet.create({})